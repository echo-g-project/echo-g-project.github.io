# ECHO-G · 恢复原始视频顺序，12 号截到 11 秒

## 数量核对（请先读）

本次能实际读取的最初 `ECHO-G_web_resources.zip` 里，`real-robot-videos/` 包含 1.mp4 至 15.mp4，共 15 条，不包含第 16 条真机视频。
此补丁恢复这 15 条真机视频，按原始文件编号 1–15 排列；加顶部独立的 long-video，共 16 个主视频/真机案例。三模态对比视频仍独立保留，不拿它填充 Trial 列表。
如果“恢复 16 条”是指真机列表本身需要 16 条，还需要另行提供第 16 条素材。本补丁没有复制其他视频或制造缺失条目的占位播放器。

## 本次修改

- 恢复之前删除的源文件 3.mp4、5.mp4 和 14.mp4。
- Trial 01–15 现在分别对应原始 1.mp4–15.mp4，不沿用删减后重排的临时显示编号。
- 原始 12.mp4 从开始保留到 11.00 秒；画面与声音均截到 11 秒，30 fps、330 帧，不变速。
- 保留 9.mp4 已完成的“原第 4 秒到结束”版本，没有恢复原长，也没有再次截去 4 秒。
- 保留 15.mp4 已完成的“原开始到第 15 秒”版本。
- 视频 9、15 与 v3 内文件字节一致。本补丁不包含它们，不会覆盖既有剪辑。
- 真机视频默认仍先显示 6 条，可展开查看全部。
- 既有大字号、表格箭头、比较区顺序与主视频不变。

## 更新方法

1. 解压本补丁。
2. 将 `index.html`、`main.js`、`gallery-restoration-v6.js` 复制到本地网站仓库根目录，替换同名文件。
3. 将补丁 `assets/` 内文件合并到仓库现有 `assets/`，替换相同路径。不要删除其他视频。
4. 保留 `site-config.js`、`styles.css`、`readability-v5.css`、`refinements-v5.js`、`data/` 和其他资源。
5. GitHub Desktop：检查 Changes → Commit → Push。
6. 发布后硬刷新，展开真机画廊，确认 Trial 01–15 连续，12 号显示 0:11；9 号约 0:17，15 号 0:15。

`gallery-restoration-v6.js` 在现有配置后、主脚本前运行，只合并画廊数据，不覆盖已填写的 Paper/Code/Dataset 链接、作者、摘要或表格。
原先在 main.js 中删除源 14.mp4 的过滤代码已经撤销。
即使此前物理删除了 3、5 或 14 号文件，本补丁也已包含可播放文件和封面。
若后续提供第 16 条，可在原始列表中添加对应素材，本版按源文件数字排序，不会将新条目强行删除。

## 检查情况

已检查桌面和手机无页面横向溢出、15 条的顺序与显示编号、展开/收起、视频播放与自动暂停其他视频、保留的 9/15 剪辑及 12 号精确时长、表格数据与模态顺序保持不变。
这是本地更新包，没有自动提交或修改线上仓库。
